<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ticket Enviado</title>
  <style>
    body {
      background-color: #f3f3f3;
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }

    .ticket {
      background-color: #fff;
      padding: 20px;
      border-radius: 5px;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
      text-align: center;
    }

    h1 {
      color: #333;
      font-size: 24px;
      margin-bottom: 10px;
    }

    p {
      color: #555;
      font-size: 18px;
    }

    #timer {
      color: #ff5722;
      font-size: 24px;
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <div class="ticket">
    <h1>Ticket Enviado</h1>
    <p>Su ticket se ha enviado por correo electrónico.</p>
    <p>Redireccionando en <span id="timer">2</span> segundos...</p>
  </div>
  <script>
    let seconds = 2;
    const timerElement = document.getElementById('timer');

    const countdown = () => {
      timerElement.textContent = seconds;
      seconds--;

      if (seconds < 0) {
        window.location.href = 'http://localhost:8080/products';
      } else {
        setTimeout(countdown, 3000);
      }
    };

    countdown();
  </script>
</body>
</html>
